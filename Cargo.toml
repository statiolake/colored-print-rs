[package]
name = "colored_print"
version = "0.1.0"
authors = ["statiolake <statiolake@gmail.com>"]

[target."cfg(unix)".dependencies]
termion = "1.5.0"

[target."cfg(windows)".dependencies]
kernel32-sys = "0.2.2"
winapi = "0.2.8"
